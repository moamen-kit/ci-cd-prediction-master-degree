# Virtual environments — the only deliberate exclusion of project content
.venv/
venv/
env/
ENV/

# Machine artifacts (regenerated; committing .pyc causes stale-bytecode bugs)
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/

# Local Claude Code permissions — has historically contained a PAT (F-10)
.claude/settings.local.json

# Office lock files
~$*
